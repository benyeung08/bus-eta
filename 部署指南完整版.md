# 🚌 香港巴士實時 ETA 系統 — 完整部署指南

## 推薦方案對比

| 方案 | 難度 | CORS | 適合 |
|---|---|---|---|
| **A. GitHub Pages** | ⭐ 最簡單 | ✅ 大部分正常 | 一般用戶（推薦） |
| **B. Cloudflare Worker 代理** | ⭐⭐ 中等 | ✅ 100% 解決 | 技術用戶，追求完美 |
| **C. 本地伺服器** | ⭐ 簡單 | ⚠️ 部分 | 臨時測試 |

---

## 方案 A：GitHub Pages（推薦，3 分鐘）

### 步驟
1. 前往 [github.com](https://github.com) 註冊/登入
2. 點右上角 `+` → **New repository**
3. 命名 `bus-eta`，設為 **Public**，勾選 Add README
4. 進入 repo → **Add file** → **Upload files**
5. 上傳 `index.html` + `manifest.json`
6. **Settings** → 左側 **Pages** → Source: `Deploy from a branch` → Branch: `main` → Save
7. 等 1-2 分鐘 → 訪問 `https://你的用戶名.github.io/bus-eta/`

### 預期結果
- ✅ KMB API：正常（GitHub Pages 的 https 來源被接受）
- ✅ CTB V2 API：正常
- ✅ HKO 天文台：正常
- ⚠️ 運輸署 XML：可能 CORS 受限（降級模式，不影響主要功能）

---

## 方案 B：Cloudflare Worker 代理（100% 解決 CORS）

如果 GitHub Pages 上仍有個別 API 報 CORS 錯誤，用此方案完全繞過。

### 步驟
1. 前往 [workers.cloudflare.com](https://workers.cloudflare.com) 註冊（免費）
2. **Create Worker** → 名稱填 `bus-proxy`
3. 清空編輯器，**貼上 `cloudflare-worker.js` 的內容**
4. 點 **Deploy** → 得到 URL：`https://bus-proxy.你的帳號.workers.dev`
5. 打開你的巴士 ETA 網站
6. 按 F12 打開開發者工具 → Console 標籤
7. 輸入：
```javascript
localStorage.setItem('bus-proxy','https://bus-proxy.你的帳號.workers.dev')
```
8. 重新整理頁面

### 原理
```
瀏覽器 → Worker 代理 → 政府 API
         (加 CORS headers)
```
Worker 位於雲端，不受瀏覽器 CORS 限制，返回時自動加上 `Access-Control-Allow-Origin: *`。

### 免費額度
- 每日 100,000 次請求（綽綽有餘）
- 無需信用卡

---

## 方案 C：本地伺服器（臨時測試）

```bash
# 下載 index.html 後：
cd ~/Downloads
python3 -m http.server 8080
# 瀏覽器開 http://localhost:8080/
```

---

## ❓ 故障排除

### KMB/CTB 仍報 422？
1. 確認網址是 `https://`（不是 `http://`）
2. 打開 F12 → Network 標籤 → 點擊失敗的請求 → 查看 Response
3. 若顯示 "Request blocked by AWS WAF" → 設置 Cloudflare Worker 代理（方案 B）

### 運輸署數據空白？
正常。運輸署 XML 不開放 CORS，系統會自動降級到九巴 RSS + 天氣關聯分析。

### 手機上怎麼用？
部署到 GitHub Pages 後：
- **iOS**：Safari 分享 → 加入主屏幕
- **Android**：Chrome 選單 → 安裝應用

系統已配置 PWA manifest，可離線緩存常用路線。

---

## 📁 文件說明

| 文件 | 用途 |
|---|---|
| `index.html` | 主程序（1890 行，含全部 JS） |
| `manifest.json` | PWA 配置 |
| `cloudflare-worker.js` | CORS 代理（可選，完美方案用） |
| `deploy-to-github.sh` | 一鍵部署腳本（可選） |

---

## 🔒 隱私說明

- 系統純前端，無後端服務器
- 不收集任何個人資料
- API 請求直接從瀏覽器發送到政府服務器
- localStorage 只存：語言偏好、主題、路線緩存
- 若使用 Cloudflare Worker，流量經 Cloudflare 中轉（匿名）
